runtime: python39

entrypoint: gunicorn -b :$PORT index:app

instance_class: F2

env_variables:
  AWS_SHARED_CREDENTIALS_FILE: "./cred"

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto
